<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude device credentials from cloud backup -->
    </cloud-backup>
    <device-transfer>
        <!-- Exclude device credentials from device-to-device transfer -->
    </device-transfer>
</data-extraction-rules>